文件功能：说明 Backend 数据库迁移目录的封版基线策略。

# Backend 数据库迁移

当前迁移链保留两个合并节点，既支持空数据库初始化，也保留 2026-07-30 基线后的数据修复过程。

- `versions/20260730_0100_font_family_entities.py` 是发布基线迁移，包含截至 2026-07-30 的完整结构。
- `versions/20260809_0100_ai_attachment_asset_lifecycle.py` 是当前唯一 head，包含基线后的结构调整与数据修复。
- 其余开发期 revision 已按原执行顺序合并到上述两个节点，不再作为独立迁移脚本。
- 已具备最新结构但版本号缺失的数据库，应使用 `alembic stamp 20260809_0100` 对齐；处于 `20260730_0100` 基线的数据库可直接执行 `alembic upgrade head`。
