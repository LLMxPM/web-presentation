文件功能：说明 Backend 数据库迁移目录的封版基线策略。

# Backend 数据库迁移

当前迁移面向首次发布，从空数据库直接创建当前开发库对应的最终结构。

- `versions/20260626_0111_initial_release_schema.py` 是唯一保留的发布基线迁移。
- 历史开发期增量迁移已合并，不再作为发布链路的一部分。
- 当前开发库 `alembic_version` 已对齐到 `20260626_0111`；若已有数据库结构已具备最新结构但版本号缺失，应使用 `alembic stamp 20260626_0111` 对齐版本号。
